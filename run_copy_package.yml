- name: copy packages
  hosts: all
  become: false

  tasks: 
 
  - name: run some command  
    command: "ls -Al /mnt1"
    delegate_to: localhost

  - name: run some command  
    command: "ls -Al /runner/project"
    delegate_to: localhost

  - name: run some command  
    command: "touch /mnt2/hello.txt"
    delegate_to: localhost


  - name: run some command  
    command: "ls -Al /mnt2"
    delegate_to: localhost


  - name: first task
    ansible.builtin.copy:
      src: ./client/linux-x86_64
      dest: /mnt2
